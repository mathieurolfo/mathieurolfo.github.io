<?xml-stylesheet type="text/css" href="../css/main.css"?>



<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" version="1.1">
	<defs>
	  <style type="text/css">@import url(http://fonts.googleapis.com/css?family=Rubik:300);</style>
	</defs>

	<circle cx="240" cy="125" r="120" stroke="white" stroke-width="2" fill-opacity="0.25" fill="white" onmouseover="evt.target.setAttribute('fill-opacity', '0.5')" onmouseout="evt.target.setAttribute('fill-opacity', '0.25')" onclick="click1()"/>

	<circle cx="125" cy="125" r="120" stroke="white" fill="white" stroke-width="2" fill-opacity="0.25" onmouseover="evt.target.setAttribute('fill-opacity', '0.5')" onmouseout="evt.target.setAttribute('fill-opacity', '0.25')" onclick="click2()"/>

	

	<a href="/projects" target="_parent">
		<text x="38" y="130" fill="white" onclick="click1" margin="5px">code</text>
	</a>

	<a href="/projects" target="_parent">
		<text x="145" y="130" fill="white" >design</text>
	</a>

	<a href="/projects" target="_parent">
		<text x="265" y="130" fill="white" >people</text>
	</a>
	<script type="text/javascript">
		function click1() {
			console.log("1 clicked");
		};

		function click2() {
			console.log("2 clicked");
		}
	</script>

	<!-- <path id="arc1" fill="white" stroke="white" stroke-width="2" opacity="0.5"/> -->
	<!-- <script type="text/javascript">
	//<![CDATA[
		document.getElementById("arc1").setAttribute("d", mapArc(155, 105, 100, 0, 220));

		function polarToCartesian(centerX, centerY, radius, angleInDegrees) {
			var angleInRadians = (angleInDegrees-90) *Math.PI / 180.0;
			return {
				x: centerX + (radius*Math.cos(angleInRadians)),
				y: centerY + (radius*Math.sin(angleInRadians))
			};
		};

		function mapArc(x, y, radius, startAngle, endAngle) {
			var start = polarToCartesian(x, y, radius, endAngle);
			var end = polarToCartesian(x, y, radius, startAngle);
			var arc = endAngle - startAngle <= 180 ? "0" : "1";
			var d = [
				"M", start.x, start.y, 
				"A", radius, radius, 0, arc, 0, end.x, end.y,
				"L", x, y, 
				"L", start.x, start.y
			].join(" ");
			return d;
		}
	//]]>
	</script>
 -->
</svg>

